apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: stack1
type: Opaque
stringData:
  DB_PASSWORD: "changeme"
  VAULT_ADDR: "http://vault:8200"
  VAULT_TOKEN: "replace-in-local"
  VAULT_DB_SECRET_PATH: "db-creds"
  VAULT_OPENAI_SECRET_PATH: "openai"
  # OPENAI_API_KEY: "put-your-key-here"
